import 'package:flutter/material.dart';

class HomeStateError extends StatelessWidget {
  const HomeStateError({super.key});

  @override
  Widget build(BuildContext context) => const Center(
        child: Text('Something went wrong'),
      );
}
